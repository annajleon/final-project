---
title: "STAT 184 Final Project"
subtitle: "NYC AirBnb Data"
author: "Anna Leon"
output: html_notebook
---

## Front Matter

```{r}
# clean up workspace environment
rm(list = ls())

# all packages used for the assignment
library(DataComputing)
library(readr)
```

## Data Access

```{r}
NYC <- read_csv("AB_NYC_2019.csv")

head(NYC)
names(NYC)
```

```{r}
glimpse(NYC) 
```

## Data Wrangling

```{r}
BoroughAvgPrice <-
  NYC %>%
    group_by(borough = neighbourhood_group) %>%
    summarise(avg_price = round(mean(price, na.rm=TRUE))) %>%
    arrange(desc(avg_price))
BoroughAvgPrice
```

```{r}
NYCwAvg <-
  NYC %>%
    left_join(BoroughAvgPrice, by = c("neighbourhood_group" = "borough")) %>%
    rename(avg_price_in_borough = avg_price)
NYCwAvg
```

```{r}
#insert nested ifs to fill column that says expensive, average, cheap for borough
```


## Data Visualization

```{r}

```

